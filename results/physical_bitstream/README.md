# Physical-Bitstream Results

Small result tables and summaries for the supplementary dense GOP-4 physical-bitstream experiment can be stored here.

Large payload archives are distributed through the GitHub Release `v1.0.0-thesis`, including:

- `physical_channel_gop4_fast.zip`
- `shared_anchor_channel_gop4.zip`

The reconstructed MP4 container sizes are not used as transmitted bitrate.
